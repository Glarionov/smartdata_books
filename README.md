## Simple SPA CRUD, using laravel + reactjs

Starting project after pulling:

# prepare:

- composer install
- npm i
- setup .env file
- php artisan migrate
- php artisan seed

To run app use:
- php artisan serve
- npm run watch
